{
  "name": "drboom-gatepost",
  "version": "0.1.0",
  "description": "An extension for drboom to catch errors from gatepost.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nathan Fritz",
  "license": "MIT",
  "dependencies": {}
}
